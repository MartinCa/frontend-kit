Fixtures for `test/eslint-config.test.mjs`. Each file exists to prove one rule
in the shared config fires (or stays quiet) where it should. They are linted
deliberately and are not part of the published package.
